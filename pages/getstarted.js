import React from 'react';
import GetStartedRedirect from '../components/GetStartedRedirect';

class GetStarted extends React.Component {
  render() {
    return (
      <GetStartedRedirect />
    );
  }
}

export default GetStarted;
