import React from 'react';

export default class GetStartedRedirect extends React.Component {
  componentDidMount() {
    window.location.replace('https://www.vergecurrency.com/get-verge');
  }

  render() {
    return (true);
  }
}
